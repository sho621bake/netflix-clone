<nav class="fixed top-0 z-50 w-full transition-all duration-500 bg-black/90">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- ロゴ -->
      <div class="flex items-center">
        <a href="/" class="text-red-600 font-bold text-2xl">
          MOVIEFLIX
        </a>
        <div class="hidden md:block ml-10">
          <div class="flex items-center space-x-4">
            <a href="/" class="text-white hover:text-gray-300">
              ホーム
            </a>
            <a href="/movies" class="text-gray-300 hover:text-white">
              映画
            </a>
            <a href="/series" class="text-gray-300 hover:text-white">
              シリーズ
            </a>
            <a href="/my-list" class="text-gray-300 hover:text-white">
              マイリスト
            </a>
          </div>
        </div>
      </div>
      
      <!-- 右側のコントロール -->
      <div class="flex items-center space-x-4">
        <!-- 検索フォーム -->
        <div id="search-container" class="relative">
          <form id="search-form" class="flex" action="/search" method="GET">
            <input
              id="search-input"
              type="search"
              name="q"
              placeholder="映画を検索..."
              class="w-64 bg-gray-800 border border-gray-700 text-white rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-red-500"
            />
            <button
              type="submit"
              class="ml-2 inline-flex items-center px-3 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500"
            >
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
              検索
            </button>
          </form>
        </div>
        
        <!-- ユーザープロフィール -->
        <div class="relative">
          <button 
            id="profile-button"
            class="flex items-center space-x-2 text-white focus:outline-none"
          >
            <div class="w-8 h-8 rounded-full bg-gray-700 flex items-center justify-center text-white">
              U
            </div>
          </button>
        </div>
      </div>
    </div>
  </div>
</nav>

<script>
  // 検索フォームの挙動
  const searchForm = document.getElementById('search-form');
  const searchInput = document.getElementById('search-input');

  if (searchForm && searchInput) {
    // URLパラメータから検索クエリを取得して初期値として設定
    const urlParams = new URLSearchParams(window.location.search);
    const queryParam = urlParams.get('q');
    if (queryParam) {
      (searchInput as HTMLInputElement).value = queryParam;
    }

    // フォーム送信時の処理
    searchForm.addEventListener('submit', (e) => {
      const query = (searchInput as HTMLInputElement).value.trim();
      if (!query) {
        e.preventDefault(); // 空のクエリの場合は送信をキャンセル
      }
    });
  }
</script>